#include <stdio.h>

int main() {
    int l;
    int totle;

    printf("Enter the value of l: ");
    scanf("%d", &l);

    totle = l * l;
    printf("Area of rectengle is => %d", totle);

    return 0;
}